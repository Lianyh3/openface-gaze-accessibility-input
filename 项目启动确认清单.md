# 项目启动确认清单（冻结版 v2）

更新时间：2026-03-08

## 1. 已确认信息（来自你本轮回复）

1. 系统环境：Ubuntu（无 GPU）。
2. 摄像头：可用。
3. 交互目标：全键盘 + 候选词输入。
4. 开发目录：`/home/lyh/workspace`。
5. 仓库状态：已在当前目录初始化 Git 仓库（`main` 分支）。
6. LangChain 方案：云 API。
7. Coze 方案：不进入首个可演示版本（后续扩展可选）。

## 2. 本机核查结果

1. Python：`3.10.12`。
2. C++ 编译器：`g++ 11.4.0`。
3. Go：`1.18.1`。
4. PATH 未发现官方 OpenFace 的 `FeatureExtraction` 可执行文件。
5. 存在目录：`/home/lyh/project/cpp_openface`，但该目录是自研 C++ 项目，不是官方 OpenFace 工具包。

## 3. 技术路线冻结（先定这个，不写业务代码）

1. 主线算法框架：OpenFace（关键点/头姿/baseline）+ 自研视线优化。
2. 应用层：眼控输入法（全键盘 + 候选词）。
3. AI 增强：LangChain 进入主线；Coze 先作为扩展项，不作为主线强依赖。
4. 实现语言：**Python 主控**（开发效率高，便于实验复现）；
   说明：后续如果实时性不足，再把热点模块下沉到 C++。
5. 首版交互参数默认值：
   - 标定点：9 点。
   - dwell 触发：600ms。
   - 去抖窗口：120ms（可配置）。

## 4. 当前阻塞点（开写代码前必须处理）

1. 官方 OpenFace 尚未安装或未加入 PATH。

## 5. 我建议的“先落地顺序”（仅决策，不含排期）

1. 先完成官方 OpenFace 安装与 Demo 验证（摄像头输入、CSV 输出字段完整）。
2. 冻结 LangChain 云 API 的具体 Provider、模型名、Key 注入方式（环境变量）。
3. 冻结评测基线：角误差/FPS/延迟/CER/WPM。
4. 冻结项目目录结构与配置文件格式（`yaml`）。

## 6. 决策冻结记录

1. 2026-03-08：LangChain 采用云 API。
2. 2026-03-08：Coze 不进入首个可演示版本。
